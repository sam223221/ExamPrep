# Runtime + build + test deps, pinned. Versions mirror ExamPrep/cyper so the two
# offline apps stay consistent and engineers reuse known-good code.
# torch is installed separately from the CPU wheel index (see Dockerfile) but is
# pinned here too so the version is the single source of truth.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pymupdf==1.25.1
chromadb==0.5.23
sentence-transformers==3.3.1
torch==2.5.1
markdown==3.7
bleach==6.2.0
httpx==0.28.1
pytest==8.3.4
ruff==0.8.4
